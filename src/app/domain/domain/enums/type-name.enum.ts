export enum TypeNameEnum {
  PRODUCT_REGISTERED = 'product.registered',
  PRODUCT_UPDATED = 'product.updated',
  PRODUCT_PURCHASE_REGISTERED = 'product.purchase.registered',
  CUSTOMER_SALE_REGISTERED = 'customer.sale.registered',
  SELLER_SALE_REGISTERED = 'seller.sale.registered',
  SALE_REGISTERED = 'sale.registered',

  BRANCH_REGISTERED = 'branch.registered',

  USER_REGISTERED = 'user.registered',
}
